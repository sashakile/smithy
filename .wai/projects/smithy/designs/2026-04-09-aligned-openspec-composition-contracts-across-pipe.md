Aligned OpenSpec composition contracts across pipeline, engines, middleware, cascade, and proposal workflows. Standardized stage carrier on {:ok ...} | {:fault ...} | {:escalate ...}; replaced retry-to-cascade side-channel signaling with explicit escalation results; specified deterministic associative candidate fold with law-testing requirement; split Proposal into kind-indexed lifecycle rules; replaced name-based middleware insertion with stable capability slots.
