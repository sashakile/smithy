Aligned OpenSpec and companion docs after Rule of 5 review. Resolved middleware ordering so error enrichment is inside retry and cascade-bridge observes exhausted retries; made wrap-pii-redaction mandatory on the signal-write path; added mr review, topology, status, metrics, split, and fuse command coverage; normalized proposal lifecycle to draft -> shadowing -> reviewing -> approved -> committed with emergency reprogram skipping shadowing only; clarified trace cardinality, batch envelope semantics, cascade timeout, and EMIT validation after ACT.
